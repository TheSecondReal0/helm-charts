image:
  repository: docker.io/searxng/searxng
  tag: latest

valkey:
  image: docker.io/valkey/valkey:9-alpine
  storage: 1Gi

service:
  host: searxng.asaio.net

externalSecrets:
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore

