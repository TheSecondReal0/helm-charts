kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: test-claim-nfs-test-4
  annotations:
    volume.beta.kubernetes.io/storage-class: "truenas-nfs"
spec:
  storageClassName: truenas-nfs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

